# HLE-Verified Dataset

This registry entry records the immutable snapshot used by the existing HLE
Lite runner. Data files are downloaded or mounted at runtime; no dataset
archive is committed to DataLite-RSI.

The pinned snapshot has 2,500 train items: 668 Gold, 1,143 Revision, and 689
Uncertain. The source repository does not declare a standard SPDX license in
its metadata, so this entry retains `NOASSERTION` pending upstream
clarification.
